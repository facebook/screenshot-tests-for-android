buildscript {
    repositories {
        jcenter()
        maven() {
           url "./repo"
        }
    }
}

allprojects {
    version = '0.1'
}

task repoClean << {
     delete file('repo')
}

task uploadAllLocal(dependsOn: [
     ":repoClean",
     ":plugin:uploadArchives",
     ":core:uploadArchives"]) {
}